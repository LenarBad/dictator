# Copy to keystore.properties (same folder). Sideload key — not Play Store.
# Anyone can rebuild; updates only install over APKs signed with this keystore.
storeFile=sideload.jks
storePassword=dictator-sideload
keyAlias=dictator
keyPassword=dictator-sideload
